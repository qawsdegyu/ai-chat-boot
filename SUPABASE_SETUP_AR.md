# تهيئة Supabase لمشروع IMCAN Inventory Hub

افتح مشروع Supabase المطلوب، ثم انتقل إلى **SQL Editor → New query**. انسخ محتوى الملف `supabase-init.sql` كاملاً والصقه ثم اضغط **Run**.

ينشئ الملف الجداول التالية:

| الجدول | الغرض |
|---|---|
| `inventory_records` | كل سجلات الراوتر ومعلومات الموقع وحالة الترحيل |
| `users` | المستخدمون والأدوار `user/admin` |
| `audit_logs` | سجل الاستيراد وتغيير الأدوار والإجراءات الحساسة |
| `ai_conversations` | عناوين محادثات المساعد وحالتها المؤرشفة |
| `ai_messages` | رسائل المستخدم والمساعد المرتبطة بالمحادثات |
| `inventory_dashboard` | view مبسط للقراءة من لوحة البيانات |

تم تفعيل **RLS**. القراءة العامة للـ`inventory_records` متاحة للصفحات المستقلة باستخدام مفتاح anon، أما الإدخال والتعديل والحذف وسجل النشاط والمحادثات فتحتاج مستخدم Supabase مسجلاً وسياسات ملكية مناسبة.

بعد التشغيل، تحقق من وجود الجداول من **Table Editor**. إذا أردت عرض البيانات الفعلية، استورد ملفي `NewInventory.xlsx` و`IMCAN-Reference-Sheet---2024.xlsm` من التطبيق أو أضف سجلاتك من Table Editor. ملف SQL لا يضيف بيانات وهمية ولا يحذف بيانات موجودة.

## مهم حول تسجيل الدخول

التطبيق الحالي يستخدم نظام Manus OAuth في الواجهة الأساسية، بينما سياسات RLS في هذا الملف تعتمد على `auth.uid()` الخاص بـSupabase Auth. لذلك يجب عند ربط التطبيق النهائي بأمان أن نختار أحد المسارين: استخدام Supabase Auth للمستخدمين، أو إبقاء Manus OAuth وتنفيذ القراءة من خادم التطبيق بمفتاح server-side آمن. لا تضع `service_role` داخل HTML أو JavaScript في المتصفح.

## إعدادات الاتصال

في التطبيق الكامل يجب ضبط:

```text
VITE_SUPABASE_URL=https://awoyujiumnnwwolngnop.supabase.co
VITE_SUPABASE_ANON_KEY=<مفتاح anon الكامل من Supabase API Settings>
```

مفتاح anon لا يتجاوز RLS. إذا ظهر `401`، استخدم مفتاح anon من المشروع نفسه وتأكد من عدم وجود بادئة `public` منفصلة أو مسافات.
