# حزمة مشروع IMCAN Inventory Hub

هذه الحزمة تحتوي على المصدر الكامل للمشروع، اختبارات Vitest، ملفات SQL الخاصة بـSupabase، صفحات HTML المستقلة، وثائق التنفيذ، وملفات الاعتماد اللازمة لإعادة التثبيت.

## التشغيل

بعد فك الضغط، شغّل:

```bash
pnpm install
pnpm check
pnpm test
pnpm dev
```

لتهيئة Supabase، افتح `supabase-init.sql` وانسخ محتواه إلى Supabase SQL Editor. اقرأ `SUPABASE_SETUP_AR.md` قبل التنفيذ.

لنسخة HTML المستقلة، افتح `standalone-html/index.html` مباشرة من المتصفح. هذه النسخة تدعم قراءة Excel محلياً عبر SheetJS، حفظ البيانات داخل المتصفح، البحث، التحليلات، والتصدير.

## الأمان

تم استبعاد ملفات `.env`، مفاتيح API، الأسرار، سجلات الخادم، `node_modules`، مجلد Git، وملفات الرفع الأصلية من هذه الحزمة. يجب إدخال الأسرار من إعدادات البيئة أو لوحة الاستضافة، وليس داخل الكود.

لم تُضمّن ملفات Excel الأصلية لأنها قد تحتوي على بيانات تشغيلية حساسة. إذا احتجت نقلها، انقلها بشكل منفصل عبر قناة آمنة.

## ملاحظات Versa

إذا كان Versa يستقبل مشروع Node/React، ارفع مجلد المشروع بعد فك الضغط وشغّل أمر build الموجود في `package.json`. إذا كان المطلوب صفحات ثابتة فقط، استخدم مجلد `standalone-html` وحده.
